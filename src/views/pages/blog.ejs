<%- include('../partials/nav', { active: 'blog' }) %>
<header class="py-4 bg-light border-bottom mb-4">
    <div class="container">
        <div class="text-center my-5">
            <h1 class="fw-bolder">Blog</h1>
            <p class="lead mb-0">Welcome to my blog, where I will show you my ...</p>
        </div>
    </div>
</header>
<main>
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <% for( const article of articles) { %> 
                    <%- include('../partials/post-preview', { article: article }) %>
                <% } %>
                <%- include( '../partials/pagination', { pagination: pagination } ) %>
            </div>
        </div>
    </div>
</main>