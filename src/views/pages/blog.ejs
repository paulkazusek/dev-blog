<%- include('../partials/nav', { active: 'blog' }) %>
<header class="py-4 bg-light border-bottom mb-4">
    <div class="container">
        <div class="text-center my-5">
            <h1 class="fw-bolder">Welcome to my blog</h1>
            <p class="lead mb-0">where I will show you my ...</p>
        </div>
    </div>
</header>
<main>
    <div class="container">
        <div class="row align-items-center">
            <div class="col-8 mx-auto">
                <% for( const article of articles) { %> 
                    <%- include('../partials/post-preview', { article: article }) %>
                <% } %>
            </div>
            <%- include( '../partials/pagination', { pagination: pagination } ) %>
        </div>
    </div>
</main>